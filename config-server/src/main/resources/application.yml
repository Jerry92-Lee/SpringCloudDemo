server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringCloudConfig/
          search-paths: respo
          username:
          password:
      label: master
